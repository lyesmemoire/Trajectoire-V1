# FEDERATION INVENTORY

## Federation
- **src\watchdog\federation\FederatedWatchdog.ts** – exists: true, compiles: true
  - imports: import { EventEmitter } from "events", import { NodeHealth, RestartVote } from "./types", import { WeightedQuorum } from "./WeightedQuorum", import { LeaseManager } from "./LeaseManager", import { TermManager } from "./TermManager", import { FaultGovernor } from "../../observability/FaultGovernor", import { InterNodeComm } from "./InterNodeComm", import { FaultOverlayState } from "../../observability/FaultOverlayState", import { NodePruner } from "./NodePruner", import { LeaderElection } from "./LeaderElection"
  - exports: export interface FederatedWatchdogOptions, export class FederatedWatchdog
- **src\watchdog\federation\InterNodeComm.ts** – exists: true, compiles: true
  - imports: import * as net from 'net', import { EventEmitter } from 'events', import { FederationMessage } from './types'
  - exports: export class InterNodeComm
- **src\watchdog\federation\LeaderElection.ts** – exists: true, compiles: true
  - imports: import { TermManager } from "./TermManager", import { RequestVote, VoteResponse } from "./types"
  - exports: export class LeaderElection
- **src\watchdog\federation\LeaseManager.ts** – exists: true, compiles: true
  - imports: import * as fs from "fs", import * as path from "path", import * as os from "os"
  - exports: export interface Lease, export class LeaseManager
- **src\watchdog\federation\NodeId.ts** – exists: true, compiles: false
  - imports: import fs from "fs", import crypto from "crypto"
  - exports: export class NodeId
- **src\watchdog\federation\NodePruner.ts** – exists: true, compiles: true
  - imports: import { NodeHealth } from "./types", import { FaultGovernor } from "../../observability/FaultGovernor"
  - exports: export class NodePruner
- **src\watchdog\federation\RestartCoordinator.ts** – exists: true, compiles: true
  - imports: import { RestartVote } from "./types"
  - exports: export class RestartCoordinator
- **src\watchdog\federation\TermManager.ts** – exists: true, compiles: true
  - imports: import * as fs from "fs", import * as path from "path"
  - exports: export class TermManager
- **src\watchdog\federation\types.ts** – exists: true, compiles: true
  - exports: export interface NodeHealth, export type FederationMessageType, export interface BaseFederationMessage, export interface Heartbeat, export interface RequestVote, export interface VoteResponse, export interface RestartVote, export interface LeaseRenew, export type FederationMessage
- **src\watchdog\federation\WeightedQuorum.ts** – exists: true, compiles: true
  - imports: import { FaultOverlayState } from "../../observability/FaultOverlayState", import { NodeHealth } from "./types"
  - exports: export class WeightedQuorum

## Observability
- **src\observability\FaultGovernor.ts** – exists: true, compiles: true
  - imports: import { FaultTelemetry } from "./FaultTelemetry", import { FaultOverlayState } from "./FaultOverlayState", import { FaultScoringEngine } from "./FaultScoringEngine"
  - exports: export class FaultGovernor
- **src\observability\FaultOverlayState.ts** – exists: true, compiles: true
  - exports: export interface FaultOverlayState
- **src\observability\FaultScoringEngine.ts** – exists: true, compiles: true
  - imports: import { FaultTrace } from "./FaultTelemetry", import { FaultOverlayState } from "./FaultOverlayState"
  - exports: export class FaultScoringEngine
- **src\observability\FaultTelemetry.ts** – exists: true, compiles: true
  - exports: export type FaultSeverity, export type FaultDomain, export interface FaultEvent, export interface FaultTrace, export class FaultTelemetry
- **src\observability\GlobalStateStore.ts** – exists: true, compiles: false
  - imports: import { EventStreamBus } from "../distributed/stream/EventStreamBus"
  - exports: export type GlobalStateSnapshot, export class GlobalStateStore
- **src\observability\MetricFaultBridge.ts** – exists: true, compiles: true
  - exports: export class MetricFaultBridge
- **src\observability\TelemetryBus.ts** – exists: true, compiles: true
  - exports: export const TelemetryBus

## Runtime
- **src\watchdog\federation\FederatedWatchdog.ts** – exists: true, compiles: true
  - imports: import { EventEmitter } from "events", import { NodeHealth, RestartVote } from "./types", import { WeightedQuorum } from "./WeightedQuorum", import { LeaseManager } from "./LeaseManager", import { TermManager } from "./TermManager", import { FaultGovernor } from "../../observability/FaultGovernor", import { InterNodeComm } from "./InterNodeComm", import { FaultOverlayState } from "../../observability/FaultOverlayState", import { NodePruner } from "./NodePruner", import { LeaderElection } from "./LeaderElection"
  - exports: export interface FederatedWatchdogOptions, export class FederatedWatchdog
- **src\watchdog\federation\InterNodeComm.ts** – exists: true, compiles: true
  - imports: import * as net from 'net', import { EventEmitter } from 'events', import { FederationMessage } from './types'
  - exports: export class InterNodeComm
- **src\watchdog\federation\LeaderElection.ts** – exists: true, compiles: true
  - imports: import { TermManager } from "./TermManager", import { RequestVote, VoteResponse } from "./types"
  - exports: export class LeaderElection
- **src\watchdog\federation\LeaseManager.ts** – exists: true, compiles: true
  - imports: import * as fs from "fs", import * as path from "path", import * as os from "os"
  - exports: export interface Lease, export class LeaseManager
- **src\watchdog\federation\NodeId.ts** – exists: true, compiles: false
  - imports: import fs from "fs", import crypto from "crypto"
  - exports: export class NodeId
- **src\watchdog\federation\NodePruner.ts** – exists: true, compiles: true
  - imports: import { NodeHealth } from "./types", import { FaultGovernor } from "../../observability/FaultGovernor"
  - exports: export class NodePruner
- **src\watchdog\federation\RestartCoordinator.ts** – exists: true, compiles: true
  - imports: import { RestartVote } from "./types"
  - exports: export class RestartCoordinator
- **src\watchdog\federation\TermManager.ts** – exists: true, compiles: true
  - imports: import * as fs from "fs", import * as path from "path"
  - exports: export class TermManager
- **src\watchdog\federation\types.ts** – exists: true, compiles: true
  - exports: export interface NodeHealth, export type FederationMessageType, export interface BaseFederationMessage, export interface Heartbeat, export interface RequestVote, export interface VoteResponse, export interface RestartVote, export interface LeaseRenew, export type FederationMessage
- **src\watchdog\federation\WeightedQuorum.ts** – exists: true, compiles: true
  - imports: import { FaultOverlayState } from "../../observability/FaultOverlayState", import { NodeHealth } from "./types"
  - exports: export class WeightedQuorum
- **src\watchdog\HealthServer.ts** – exists: true, compiles: false
  - imports: import * as http from "http", import { GlobalStateStore, GlobalStateSnapshot } from "../observability/GlobalStateStore"
  - exports: export class HealthServer
- **src\watchdog\ProcessProbe.ts** – exists: true, compiles: true
  - imports: import * as http from "http", import * as https from "https", import { URL } from "url"
  - exports: export class ProcessProbe
- **src\watchdog\WatchdogConfig.ts** – exists: true, compiles: true
  - exports: export interface WatchdogConfig
- **src\watchdog\WatchdogEngine.ts** – exists: true, compiles: true
  - imports: import { WatchdogConfig } from "./WatchdogConfig", import { WatchdogState } from "./WatchdogState", import { NodeHealth } from "./federation/types", import { ProcessProbe } from "./ProcessProbe", import { spawn } from "child_process", import * as fs from "fs", import * as path from "path", import * as os from "os"
  - exports: export class WatchdogEngine
- **src\watchdog\WatchdogState.ts** – exists: true, compiles: true
  - exports: export interface WatchdogState

