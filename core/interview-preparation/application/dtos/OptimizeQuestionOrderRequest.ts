/**
 * OptimizeQuestionOrderRequest DTO
 *
 * Request DTO for optimizing question order.
 * NO business logic, NO reasoning, NO analysis.
 * ONLY data transfer object definition.
 */

export interface OptimizeQuestionOrderRequest {
  planId: string;
  strategy: string;
}
