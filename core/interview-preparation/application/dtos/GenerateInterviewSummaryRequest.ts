/**
 * GenerateInterviewSummaryRequest DTO
 *
 * Request DTO for generating interview summary.
 * NO business logic, NO reasoning, NO analysis.
 * ONLY data transfer object definition.
 */

export interface GenerateInterviewSummaryRequest {
  planId: string;
}
