/**
 * PreviewInterviewPlanRequest DTO
 *
 * Request DTO for previewing interview plans.
 * NO business logic, NO reasoning, NO analysis.
 * ONLY data transfer object definition.
 */

export interface PreviewInterviewPlanRequest {
  planId: string;
}
