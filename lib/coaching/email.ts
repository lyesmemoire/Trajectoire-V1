/**
 * Stub: Email broadcast utility for coaching emails.
 */
export async function sendBroadcast(to: string, subject: string, _html: string, ): Promise<void> {
  
  console.log(`[Email Stub] Would send to ${to}: ${subject}`);
}
