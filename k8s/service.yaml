apiVersion: v1
kind: Service
metadata:
  name: trajectoire-web
  labels:
    app: trajectoire
    component: web
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: trajectoire
    component: web